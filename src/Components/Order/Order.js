import './Order.css'

const Order = (props) => {
    // const [userDetail, setUserDetail] = useContext(UserContext)
    console.log(props)
    return (
        <div>
            <h2>Hello This Is Your Order Detail</h2>
        </div>
    );
};

export default Order;